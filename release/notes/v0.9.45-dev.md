Development-channel candidate for 5tratMux 0.9.45. The compiled runtime is identical to the stable candidate; only version metadata differs.

- Fixes external pools reappearing after removal, including the last or newest saved pool.
- Removes stale routing and AI restore references, reconnecting only affected miners.
- Shows removal progress and preserves the request during page navigation.
- Prevents delayed pool research from undoing deactivation or newer edits.
- Preserves and verifies TLS for Stratum SSL connections.
- Protects miner names from stale background telemetry saves.

Existing SHA-256 and Scrypt support, configuration and licence state are preserved.

Validation: 487 Python tests, JavaScript behavioral checks and protected-runtime/startup checks passed on native AMD64 and ARM64 builds. The AMD64 runtime was exercised on the live validation node, including pool removal during immediate full-page navigation and continued accepted mining shares.
