DEV preview of the cumulative 5tratMux 0.9.47 update, covering Fluminer onboarding, mixed SHA256/Scrypt AI portfolios and Scrypt job handling.

- Discover Fluminer L2 miners, try the factory `root` / `root` login first, and request credentials if needed. Read telemetry and configure pools while retaining and verifying saved fallbacks.
- Refresh AI scope as miners join and keep selected miners visible in Overview while their first decision is pending.
- Use live Stratum and accepted work for AI eligibility when management telemetry is unavailable. Prefer fresh hardware hashrate, then current accepted-work estimates, before a labelled known-capacity fallback.
- Allocate SHA256 and Scrypt independently among compatible destinations. Keep MUX responses, reserve rules, delivery counts, route percentages, probability estimates and usage forecasts consistent with each algorithm and current work.
- Preserve Scrypt difficulty/job ordering and prevent unnecessary cached-job replay on active difficulty changes. Reduce Stratum forwarding overhead by caching routing controls until settings change.
- Correct Setup's LAN address on hosts with multiple interfaces and keep route badges readable on desktop and narrow screens.

Fluminer L1 uses the shared API adapter but still needs physical-device validation. The shared Stratum port remains 7331. Earlier external-pool removal and Scrypt compatibility fixes are included.

