5tratMux 0.9.49 restores automatic MUX Attack monitoring and app-validated launches when Local 5TRATMUX is selected.

Built-in or verified rules must still satisfy the saved trust threshold, evidence persistence, route health, mission scope and cooldowns. Paid entitlement is checked for launch and continued automated allocations; expired access restores the prior portfolios. Weak signals show that the controller is waiting for local evidence.

Ordinary LLM fleet comparisons cannot create attacks. Launch authority stays with the app's validated control policy; this change does not introduce model-generated probabilities or unrestricted launch instructions.

Managed Local AI installation is available through 5tratumOS 0.8.4 on compatible Linux AMD64 hosts with the runtime's required CPU features, including AVX2. Confirmed paid users can choose Qwen3.5 0.8B, 2B or 4B and download only their selected model. The model weights come from pinned upstream revisions and are checked against fixed SHA-256 hashes. Runtime 0.1.2 corrects memory admission after multiple model downloads while retaining host reserves and container limits. Models remain experimental; basic download/startup checks do not establish equal decision quality or qualify ARM inference.

This is a native application correction. Published 5tratumOS 0.8.4 assets remain unchanged. Install 0.9.49 through the normal signed Mux updater; an already installed OS version alone does not establish that this native correction is installed. Application state, licensing identity and downloaded local models are retained.

Runtime images are compiled and browser assets are minified. Private application source is not included in this public repository or its native release images.
